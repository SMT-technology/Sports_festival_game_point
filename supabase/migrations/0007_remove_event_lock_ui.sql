-- ============================================================================
-- 입력 잠금(is_locked) 기능을 관리자 화면 UI에서 제거함.
--
-- 이미 교사는 최종 제출 후 스스로 점수를 수정할 수 없도록 DB 레벨에서
-- 막혀 있어(0005 마이그레이션), "입력 잠금" 토글은 중복 기능이라 UI에서
-- 뺐다. 혹시 테스트 중 잠가둔 종목이 있다면, 이제 그걸 풀 수 있는 UI가
-- 없으므로 안전하게 전부 잠금 해제해둔다. (관리자는 원래 잠금과 무관하게
-- 항상 수정 가능했으므로 데이터에는 영향 없음)
-- ============================================================================

update public.events set is_locked = false;
